Credit of SimpleXLSX.php file => shuchkin(Github) => https://github.com/shuchkin
Source of SimpleXLSX.php file => https://github.com/shuchkin/simplexlsx

How it works?
The user inputs an excel file along with the string that is to be searched. The inputted files are saved by the name "temp" in the folder ExcelFiles(New input results in the old file to be deleted), and then the file is read and required result is displayed.